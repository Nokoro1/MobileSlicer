Depth prior model slot for local scanner.

Expected optional asset:

  depth_anything_v2_small.tflite

Use only as a relative-depth prior for guidance, preview, and reconstruction
weighting. Do not treat monocular AI depth as metric truth. Metric scale must
come from verified markers, measured calibration, AR/depth evidence, or another
explicit scale source.

Do not bundle Depth Anything V2 Base, Large, or Giant without a separate
commercial license review.
