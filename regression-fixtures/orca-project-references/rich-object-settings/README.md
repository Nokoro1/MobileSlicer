# Rich Object Settings Orca Project Fixture

Generated by `scripts/generate_orca_rich_project_fixture.sh`.

- Orca binary: `/home/peanut/Development/MobileSlicer/vendor/orcaslicer/build/package/bin/orca-slicer`
- Source artifact directory: `/home/peanut/Development/MobileSlicer/artifacts/orca-project-fixtures/rich-object-settings-20260515-121137`
- Package: `rich-object-settings.gcode.3mf`

This fixture proves desktop Orca preserves named objects, two filament
assignments, project/package thumbnails, two plate records, and object-scoped
process metadata in `Metadata/model_settings.config`.

It does not claim parameter-modifier coverage. The local Orca CLI path used
here exposes object print params through `load_assemble_list`, but not
modifier-volume creation.

It also intentionally excludes assemble-list `height_ranges` for sliced export.
The local desktop Orca CLI accepts height ranges for project-only export but
segfaults before writing diagnostics when height ranges are combined with
`--slice` and `--export-3mf`. This fixture is therefore scoped to the
strongest sliced package contract the local Orca CLI can reliably generate.
